#include<stdio.h>

int main(){

  int ventas, comision;

  ventas=5000000;

  printf("Comision= %d", (ventas > 150000) ? comision =100 : comision=0);

 //en la condicional se está ocupando la variale comision
	return 0;
}